/**
 * @param {number} N
 * @return {boolean}
 */
 
// this solution without understanding the problem properly 
// i tried this wtih my intuition and surprisingly it solved 
var divisorGame = function(N) {
    if(N % 2 === 0) return true;
    return false;
};
