/**
 * @param {number} n
 * @return {boolean}
 */
var isPowerOfTwo = function(n) {
    if(n === 1) return true;
    
    let power = 2; 
    
    while(power <= n) {
        if(n === power) {
            return true;
        }
        power *= 2;
    }
    
    return false;
};
